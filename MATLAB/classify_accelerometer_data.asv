function label = classify_accelerometer_data(X)
    model = loadLearnerForCoder('SVM_Accelerometer_Model');
    [label,score,node,cnum] = predict(model,X);
    [label,NegLoss,PBScore] = predict(___)
end